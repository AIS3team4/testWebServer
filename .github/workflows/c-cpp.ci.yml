name: C/C++ CI

on:
  push:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: testDockerImage
      uses: ./.github/actions/container/UniteTest
      with:
        entryPoint: ls
